.container
  .row
    .col-md-4.col-md-offset-4
      = simple_form_for :send, url: send_pass_token_path do |f|
        = f.error_notification
        .form-inputs
          = f.input :email, required: true, autofocus: true
          
        .form-actions
          = f.button :submit, "Отправить"